# Basics

To start with Rust you'll need the compiler, go to [rustup.rs](https://rustup.rs/) and follow the instructions.

<!-- what about Rust Rover? -->

Then I recommend installing and using Intellij CLion as you're already familiar with Intellij IDEs, otherwise you can use VSCode.

### Command line basics

Rust is controlled by its package manager `cargo`.

To run your program, execute `cargo run`

To test, it's `cargo test`
